========================================
PROMPT FOR DELTA EXCHANGE AI SUPPORT
========================================

I am building a trading bot that needs to fetch real-time Unrealized P&L (UPNL) data from Delta Exchange.

CURRENT SITUATION:
==================

I have 3 open positions on Delta Exchange:
1. C-BTC-129000-281125 (Call option): Size -0.036 BTC
2. P-BTC-92000-281125 (Put option): Size -0.075 BTC  
3. BTCUSD (Perpetual): Size +0.008 BTC

My Delta Exchange web UI shows:
- Σ Total UPNL: +$74.52 USD (PROFIT - shown in green)
- Individual position UPNLs:
  * Position 1: +$9.57
  * Position 2: +$50.57
  * Position 3: +$17.67
  * Total: $9.57 + $50.57 + $17.67 ≈ $77.82 (close to $74.52)

PROBLEM:
========

I've tried multiple API methods but NONE match the $74.52 value my UI shows:

METHOD 1: GET /v2/wallet/balances
Result: net_equity - balance = -$70.82 USD (LOSS)
Issue: This is OPPOSITE sign from UI!

METHOD 2: GET /v2/positions?underlying_asset_symbol=BTC
Result: Returns positions but unrealized_pnl field is always $0.00
Issue: No UPNL data in response!

METHOD 3: WebSocket portfolio_margins → positions_upl
Result: Shows -$69.70 USD (LOSS)  
Issue: Again OPPOSITE sign from UI!

WHAT I NEED:
============

Please provide the EXACT method (REST API endpoint or WebSocket channel) to fetch:

1. Individual position UPNL for each open position
   - Should return +$9.57, +$50.57, +$17.67 (matching UI)
   
2. Total UPNL across all positions
   - Should return +$74.52 USD (matching UI "Σ Total UPNL")

REQUIREMENTS:
=============

✅ Must return position-level unrealized_pnl with correct values
✅ Must support mixed positions (options + perpetuals)
✅ Must match what's shown in Delta Exchange web UI
✅ Prefer REST API, but WebSocket is fine too
✅ Need Python code example with authentication

MY CREDENTIALS:
===============

- API Key: hQCNEUH7... (LIVE account, India region)
- Trading on: https://api.india.delta.exchange
- WebSocket: wss://socket.india.delta.exchange
- Authentication: HMAC-SHA256 (working correctly)

WHAT I'VE TRIED:
================

REST Endpoints Tested:
❌ /v2/positions (returns size/entry but unrealized_pnl=0)
❌ /v2/wallet/balances (gives opposite sign UPNL)
❌ /v2/positions_margined (404 - doesn't exist)
❌ /v2/portfolio (404 - doesn't exist)  
❌ /v2/margins (404 - doesn't exist)

WebSocket Channels Tested:
❌ portfolio_margins → positions_upl (gives opposite sign)

QUESTION:
=========

What is the CORRECT API endpoint or WebSocket channel to get position-level UPNL 
that matches the "Σ Total UPNL" value shown in my Delta Exchange web UI?

Please provide:
1. Exact endpoint/channel name
2. Request parameters needed
3. Response field that contains UPNL
4. Python code example
5. Explanation of any calculations needed

My current implementation uses positions_upl from portfolio_margins WebSocket, 
but it shows -$69.70 while my UI shows +$74.52. Why is the sign opposite?

Is there a difference between:
- positions_upl (WebSocket field)
- unrealized_pnl (positions array)
- Σ Total UPNL (UI display)

Thank you!

========================================
COPY EVERYTHING ABOVE AND SEND TO DELTA EXCHANGE AI
========================================

