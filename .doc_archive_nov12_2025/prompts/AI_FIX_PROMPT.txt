FIX THESE TWO ISSUES IN WORKINGBOT PROJECT:

═══════════════════════════════════════════════════════════════════════════
ISSUE #1: Bot Can't Start - ModuleNotFoundError: No module named 'bot'
═══════════════════════════════════════════════════════════════════════════

PROBLEM:
When running: python3 bot/run.py
Error: ModuleNotFoundError: No module named 'bot'

ROOT CAUSE:
bot/run.py tries to import bot modules (line 16) but project root is not in sys.path yet.

CURRENT CODE (bot/run.py lines 10-16):
```python
# Add project root to Python path FIRST (before any bot imports)
project_root = Path(__file__).parent.parent.absolute()
if str(project_root) not in sys.path:
    sys.path.insert(0, str(project_root))

# Now import bot modules
from bot.config.aliases import ALIAS_TO_CANONICAL, upgrade_mapping
```

THE FIX DOESN'T WORK BECAUSE:
Line 16 still tries to import before path is added properly.

SOLUTION NEEDED:
Move the sys.path addition BEFORE any bot imports, or add the project root to sys.path in a way that persists.

CURRENT LOCATION: bot/run.py line 10-16

═══════════════════════════════════════════════════════════════════════════
ISSUE #2: Emergency Kill Not Stopping Bots from WebUI
═══════════════════════════════════════════════════════════════════════════

PROBLEM:
When clicking "EMERGENCY KILL ALL" button in WebUI (http://localhost:5555),
the bots don't stop.

ROOT CAUSE:
webui/backend/app.py can't import bot.emergency_kill module because PYTHONPATH isn't set.

CURRENT CODE (webui/backend/app.py lines 6321-6329):
```python
try:
    # Add project root to path for import
    import sys
    from pathlib import Path
    project_root = Path(__file__).parent.parent.parent.absolute()
    if str(project_root) not in sys.path:
        sys.path.insert(0, str(project_root))
    
    from bot.emergency_kill import emergency_kill_all as centralized_kill
    result = centralized_kill()
```

THE FIX DOESN'T WORK BECAUSE:
webui runs from webui/backend/app.py, so Path(__file__).parent.parent.parent points to wrong location.

PROJECT STRUCTURE:
WorkingBot/
  ├── bot/
  │   ├── run.py
  │   ├── emergency_kill.py  ← This file exists
  │   └── guardian/
  └── webui/
      └── backend/
          └── app.py  ← You are here, need to get to WorkingBot/ root

SOLUTION NEEDED:
Fix the path calculation. From webui/backend/app.py:
- Path(__file__) = /path/to/WorkingBot/webui/backend/app.py
- Path(__file__).parent = /path/to/WorkingBot/webui/backend
- Path(__file__).parent.parent = /path/to/WorkingBot/webui  
- Path(__file__).parent.parent.parent = /path/to/WorkingBot  ← CORRECT

But might need to use absolute path directly.

CURRENT LOCATION: webui/backend/app.py lines 6322-6326

═══════════════════════════════════════════════════════════════════════════
WHAT I ALREADY TRIED (DIDN'T WORK):
═══════════════════════════════════════════════════════════════════════════

1. ✅ Modified bot/run.py to add project root to sys.path BEFORE imports
2. ✅ Modified webui/backend/app.py to add project root for emergency_kill import
3. ✅ Added PYTHONPATH export to start_webui.sh
4. ❌ Still getting ModuleNotFoundError
5. ❌ Emergency Kill still can't import module

LAUNCHAGENT BOTS RUNNING:
- com.gridbot.tmux.control.plist (starts tmux session with bots)
- com.gridbot.webui.plist (starts webui backend)

LOG FILES:
- /Users/shailendrasinghrajawat/Projects/WorkingBot/logs/trading.log
- /Users/shailendrasinghrajawat/Projects/WorkingBot/logs/guardian.log

REQUIREMENTS:
1. Fix BOTH issues WITHOUT creating new files
2. Use existing scripts (start_tmux_daemon.sh, start_webui.sh)
3. Hot reload should work instantly (already coded in iv_rv_tracker.py)
4. Emergency Kill should stop all bots from WebUI button

TEST COMMANDS:
1. python3 bot/run.py - should start without ModuleNotFoundError
2. Click "EMERGENCY KILL ALL" in WebUI - should stop all bots
3. Edit grid_config.env - should reload instantly (hot reload)

FILES TO FIX:
- bot/run.py (lines 1-25)
- webui/backend/app.py (lines 6319-6350)
- scripts/start_tmux_daemon.sh (already has PYTHONPATH, but verify it works)

BOT MODULES THAT MUST WORK:
- bot.config.aliases
- bot.emergency_kill
- bot.volatility.iv_rv_tracker (already has hot reload code)

GOAL:
Fix PYTHONPATH issue so bot starts and emergency kill works, WITHOUT creating unnecessary files or scripts.

