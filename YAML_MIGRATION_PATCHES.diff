# YAML MIGRATION PATCHES
# These patches complete the WebUI migration to YAML config
# Apply with: patch -p1 < YAML_MIGRATION_PATCHES.diff

# ==============================================================================
# Patch 1: Add WebUI configuration to config.yaml
# ==============================================================================

--- a/config.yaml
+++ b/config.yaml
@@ -183,6 +183,31 @@ webui:
   allowed_origins: http://localhost:*,http://127.0.0.1:*,http://100.107.230.67:*,http://mymac.tail289dc3.ts.net:*,http://*.ts.net:*,http://100.*.*.*:*
   enabled: true
   auth_enabled: false
+  flask:
+    debug: false
+    host: 127.0.0.1
+    port: 5001
+    env: production
+  errors:
+    collector_enabled: true
+    db_path: data/errors.db
+    rate_limit_window: 60
+    rate_limit_max: 100
+    burst_threshold: 10
+    allow_destructive: false
+  logs:
+    trading_bot: bot.log
+    guardian_bot: logs/guardian.log
+    health_bot: logs/health.log
+  auth:
+    enabled: false
+    user: admin
+    # password stored in secrets/.env for security
+  pm2:
+    enabled: false
+  url:
+    default: http://localhost:5555
+
 risk_limits:
   max_account_loss_inr: 25000
   usd_to_inr_rate: 85.0

# ==============================================================================
# Patch 2: Migrate webui/backend/config.py to YAML
# ==============================================================================

--- a/webui/backend/config.py
+++ b/webui/backend/config.py
@@ -72,9 +72,13 @@ CORS_EXPOSE_HEADERS = [
     'Content-Range'
 ]
 
-DEBUG = os.getenv('FLASK_DEBUG', 'False').lower() == 'true'
-HOST = os.getenv('FLASK_HOST', '127.0.0.1')
-PORT = int(os.getenv('FLASK_PORT', 5001))
+# Load from YAML config
+from config.loader import get_config
+cfg = get_config()
+
+DEBUG = cfg.webui.flask.debug
+HOST = cfg.webui.flask.host
+PORT = cfg.webui.flask.port
 
 # Bot process management
 BOT_SCRIPT = os.path.join(BASE_DIR, 'bot', 'run_gridbot_async.py')
@@ -97,7 +101,7 @@ LOG_FORMAT = '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
 LOG_DATE_FORMAT = '%Y-%m-%d %H:%M:%S'
 
 # Logging level
-LOG_LEVEL = os.getenv('LOG_LEVEL', 'INFO')
+LOG_LEVEL = cfg.logging.level if hasattr(cfg, 'logging') and hasattr(cfg.logging, 'level') else 'INFO'
 
 # API Configuration
 API_VERSION = 'v1'

# ==============================================================================
# Patch 3: Migrate webui/backend/app.py to YAML
# ==============================================================================

--- a/webui/backend/app.py
+++ b/webui/backend/app.py
@@ -102,6 +102,10 @@ from webui.backend.routes.yaml_config_api import yaml_config_blueprint
 # Initialize Flask app
 app = Flask(__name__, static_folder=None)
 
+# Load YAML config
+from config.loader import get_config
+cfg = get_config()
+
 app.config['SECRET_KEY'] = os.getenv('FLASK_SECRET_KEY', 'dev-secret-key')
 app.config['JSON_SORT_KEYS'] = False
 
@@ -114,7 +118,7 @@ def is_valid_origin(origin):
     """Check if origin is in allowed list"""
     return origin is not None
 
-ALLOWED_ORIGINS = os.getenv('WEBUI_ALLOWED_ORIGINS', 'http://localhost:*')
+ALLOWED_ORIGINS = cfg.webui.allowed_origins
 
 @app.after_request
 def after_request(response):
@@ -423,7 +427,7 @@ def configure_error_handlers(app):
 
 if __name__ == '__main__':
     # Get port from environment or use default
-    WEBUI_PORT = int(os.getenv('WEBUI_PORT', '5555'))
+    WEBUI_PORT = cfg.webui.port
     
     print(f"🌐 Starting WebUI Backend Server")
     print(f"📍 Port: {WEBUI_PORT}")
@@ -451,7 +455,7 @@ if __name__ == '__main__':
     
     try:
         # Get Flask environment
-        flask_env = os.getenv('FLASK_ENV', 'development')
+        flask_env = cfg.webui.flask.env
         
         if flask_env == 'development':
             print(f"🔧 Running in DEVELOPMENT mode")

# ==============================================================================
# Patch 4: Migrate webui/backend/errors/manager.py to YAML
# ==============================================================================

--- a/webui/backend/errors/manager.py
+++ b/webui/backend/errors/manager.py
@@ -17,6 +17,9 @@ from webui.backend.errors.classifier import ErrorClassifier
 from webui.backend.errors.remediator import ErrorRemediator
 from bot.observability.errors.store import ErrorStore
 
+# Load YAML config
+from config.loader import get_config
+
 logger = logging.getLogger(__name__)
 
 
@@ -56,19 +59,22 @@ class ErrorManager:
         Returns:
             dict: Error manager configuration
         """
+        cfg = get_config()
+        
         config = {
-            'enabled': os.getenv('ERROR_COLLECTOR_ENABLED', 'true').lower() == 'true',
-            'db_path': os.getenv('ERROR_DB_PATH', 'data/errors.db'),
-            'config_path': os.getenv('GRID_CONFIG_PATH', 'grid_config.env'),
-            'allow_destructive': os.getenv('ALLOW_DESTRUCTIVE_FIXES', 'false').lower() == 'true',
+            'enabled': cfg.webui.errors.collector_enabled,
+            'db_path': cfg.webui.errors.db_path,
+            'config_path': 'config.yaml',  # Always use YAML now
+            'allow_destructive': cfg.webui.errors.allow_destructive,
             'log_files': {
-                'trading': os.getenv('TRADING_BOT_LOG', 'bot.log'),
-                'guardian': os.getenv('GUARDIAN_BOT_LOG', 'logs/guardian.log'),
-                'health': os.getenv('HEALTH_BOT_LOG', 'logs/health.log')
+                'trading': cfg.webui.logs.trading_bot,
+                'guardian': cfg.webui.logs.guardian_bot,
+                'health': cfg.webui.logs.health_bot
             },
-            'rate_limit_window': int(os.getenv('ERROR_RATE_LIMIT_WINDOW', '60')),
-            'rate_limit_max': int(os.getenv('ERROR_RATE_LIMIT_MAX', '100')),
-            'burst_threshold': int(os.getenv('ERROR_BURST_THRESHOLD', '10'))
+            'rate_limit_window': cfg.webui.errors.rate_limit_window,
+            'rate_limit_max': cfg.webui.errors.rate_limit_max,
+            'burst_threshold': cfg.webui.errors.burst_threshold
         }
         
         return config
@@ -168,16 +174,22 @@ class ErrorManager:
         Get runtime context for error analysis
         """
         try:
+            cfg = get_config()
+            
             return {
-                'trading_mode': os.getenv('TRADING_MODE', 'unknown'),
+                'trading_mode': cfg.trading_mode,
                 'timestamp': datetime.now().isoformat(),
                 'bot_config': {
-                    'max_open': os.getenv('GRIDBOT_MAX_OPEN', 'unknown'),
-                    'execute_orders': os.getenv('EXECUTE_ORDERS', 'unknown'),
-                    'post_only': os.getenv('POST_ONLY_MODE', 'unknown')
+                    'max_open': cfg.grid.limits.max_open_positions,
+                    'execute_orders': cfg.safety.execute_orders,
+                    'post_only': cfg.order_execution.post_only_mode
                 },
                 'guardian': {
-                    'guardian_enabled': os.getenv('GUARDIAN_ENABLED', 'unknown'),
-                    'max_loss': os.getenv('GUARDIAN_MAX_ACCOUNT_LOSS_INR', 'unknown')
+                    'guardian_enabled': cfg.guardian.enabled if hasattr(cfg, 'guardian') else False,
+                    'max_loss': cfg.guardian.max_account_loss_inr if hasattr(cfg, 'guardian') else 0
                 }
             }

# ==============================================================================
# Patch 5: Migrate webui/backend/utils/auth.py to YAML
# ==============================================================================

--- a/webui/backend/utils/auth.py
+++ b/webui/backend/utils/auth.py
@@ -8,6 +8,9 @@ from functools import wraps
 from flask import request, jsonify, g
 import hashlib
 
+# Load YAML config
+from config.loader import get_config
+
 logger = logging.getLogger(__name__)
 
 
@@ -55,10 +58,12 @@ def require_auth(f):
             # Basic auth
             auth = request.authorization
             if auth:
+                cfg = get_config()
+                
                 # Check username and password
-                valid_user = os.getenv('WEBUI_AUTH_USER', 'admin')
-                valid_pass = os.getenv('WEBUI_AUTH_PASSWORD')
+                valid_user = cfg.webui.auth.user
+                # Password still from env for security
+                valid_pass = os.getenv('WEBUI_AUTH_PASSWORD')
                 
                 if auth.username == valid_user and valid_pass and auth.password == valid_pass:
                     g.auth_user = auth.username

# ==============================================================================
# Patch 6: Migrate webui/backend/utils/pm2_adapter.py to YAML
# ==============================================================================

--- a/webui/backend/utils/pm2_adapter.py
+++ b/webui/backend/utils/pm2_adapter.py
@@ -40,6 +40,9 @@ import subprocess
 import json
 import logging
 from pathlib import Path
+
+# Load YAML config
+from config.loader import get_config
 from dotenv import load_dotenv
 
 # Load environment
@@ -66,7 +69,8 @@ except subprocess.CalledProcessError:
     HAS_PM2 = False
 
 # Check if PM2 is enabled in config
-USE_PM2 = os.getenv('USE_PM2', 'false').lower() in ('true', '1', 'yes')
+cfg = get_config()
+USE_PM2 = cfg.webui.pm2.enabled if hasattr(cfg, 'webui') and hasattr(cfg.webui, 'pm2') else False
 
 # Final determination
 PM2_AVAILABLE = HAS_PM2 and USE_PM2

# ==============================================================================
# Patch 7: Migrate services/notifications/telegram_error_notifier.py to YAML
# ==============================================================================

--- a/services/notifications/telegram_error_notifier.py
+++ b/services/notifications/telegram_error_notifier.py
@@ -13,6 +13,9 @@ import aiohttp
 import asyncio
 from typing import Optional, Dict, Any, List
 
+# Load YAML config
+from config.loader import get_config
+
 logger = logging.getLogger(__name__)
 
 
@@ -348,7 +351,8 @@ class TelegramErrorNotifier:
         """
         try:
             # Include WebUI link
-            webui_url = os.getenv("WEBUI_URL", "http://localhost:5555")
+            cfg = get_config()
+            webui_url = cfg.webui.url.default if hasattr(cfg, 'webui') and hasattr(cfg.webui, 'url') else "http://localhost:5555"
             
             # Enhanced error message with context
             message = (

# ==============================================================================
# Patch 8: Update config/schema.yaml with WebUI section
# ==============================================================================

--- a/config/schema.yaml
+++ b/config/schema.yaml
@@ -1100,6 +1100,89 @@ webui:
       type: boolean
       required: false
       default: false
+  
+  flask:
+    type: object
+    required: false
+    properties:
+      debug:
+        type: boolean
+        required: false
+        default: false
+        description: Enable Flask debug mode
+      
+      host:
+        type: string
+        required: false
+        default: "127.0.0.1"
+        description: Flask server host
+      
+      port:
+        type: integer
+        required: false
+        default: 5001
+        validation:
+          min: 1024
+          max: 65535
+        description: Flask backend port (internal)
+      
+      env:
+        type: enum
+        values: [development, production]
+        required: false
+        default: production
+        description: Flask environment
+  
+  errors:
+    type: object
+    required: false
+    properties:
+      collector_enabled:
+        type: boolean
+        required: false
+        default: true
+      
+      db_path:
+        type: string
+        required: false
+        default: data/errors.db
+      
+      rate_limit_window:
+        type: integer
+        required: false
+        default: 60
+      
+      rate_limit_max:
+        type: integer
+        required: false
+        default: 100
+      
+      burst_threshold:
+        type: integer
+        required: false
+        default: 10
+      
+      allow_destructive:
+        type: boolean
+        required: false
+        default: false
+  
+  logs:
+    type: object
+    required: false
+    properties:
+      trading_bot:
+        type: string
+        required: false
+        default: bot.log
+      
+      guardian_bot:
+        type: string
+        required: false
+        default: logs/guardian.log
+      
+      health_bot:
+        type: string
+        required: false
+        default: logs/health.log
+  
+  auth:
+    type: object
+    required: false
+    properties:
+      enabled:
+        type: boolean
+        required: false
+        default: false
+      
+      user:
+        type: string
+        required: false
+        default: admin
+        description: WebUI admin username
+      
+      # password: Intentionally NOT in config - must be in secrets/.env
+  
+  pm2:
+    type: object
+    required: false
+    properties:
+      enabled:
+        type: boolean
+        required: false
+        default: false
+        description: Enable PM2 process management
+  
+  url:
+    type: object
+    required: false
+    properties:
+      default:
+        type: string
+        required: false
+        default: http://localhost:5555
+        description: Default WebUI URL for links

 # ═══════════════════════════════════════════════════════════════════════════
 # RECONCILIATION

# ==============================================================================
# END OF PATCHES
# ==============================================================================
