# 🧠 THREAD → ASYNC MIGRATION COMPLETION PROMPT

You are the **lead engineer responsible for completing the async migration** of the WorkingBot Trading System.  
Below is the full **audit report** prepared by a Senior Python Systems Engineer on November 12, 2025.  
Use it as the **single source of truth** to guide your code inspection, feature restoration, and production readiness validation.

---

$(cat /Users/ssr/Projects/WorkingBot/ASYNC_CONVERSION_AUDIT_REPORT.md)

---

## TASK

1. **Read the full audit** above.
2. Inspect every async file and confirm whether each missing or partial feature (marked 🔴 or 🟡) has been implemented.
3. If missing, **implement it exactly as described** while preserving the async actor architecture.
4. Once all critical Phase 1 components are implemented (Exchange Reconciliation, TP Verification, non-blocking I/O), output a full verification summary in the same format as the audit report.
5. Conclude with a **Production-Readiness verdict** and confidence percentage.

Focus on accuracy, safety, and async integrity over speed.  
All existing features must remain operational and logically consistent.

