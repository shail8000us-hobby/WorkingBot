Perfect—here’s a ready-to-paste “prompt kit” mapped to the ROADMAP. Each item is a single message you can drop into chat when you want me to build/ship that upgrade. Replace anything in ⟨angle-brackets⟩ with your values.

⸻

Quick “index” you can use
	•	P1 Baseline hardening
	•	P2 Delta REST client + reports
	•	P3 WebSocket notifier + Telegram
	•	P4 Risk & safety rails
	•	P5 Observability
	•	P6 Tests & sims
	•	P7 Packaging & deploy
	•	P8 Strategy plugin system
	•	P9 Backtesting
	•	P10 CI/lint/docs

⸻

P1 — Baseline hardening

Prompt P1.1 – Lock dependencies + venv discipline

Ship ROADMAP.md → Phase 1 (Dependency hygiene). Add requirements.in, generate pinned requirements.txt with pip-tools, and update README with exact venv commands for macOS + Linux. Don’t ask me questions—just give me the files, the exact commands to run, and how to upgrade safely.

Prompt P1.2 – Secrets/env layout

Ship Phase 1 (Secrets). Standardize env loading: .env.runtime (bot), .env.reports (reports), .env.dev (local). Provide a tiny config.py that validates required variables and prints a redacted config on startup. Include sample files with placeholders.

Prompt P1.3 – Job runner templates

Ship Phase 1 (Scheduling). Give me launchd and systemd units for: hourly report, daily prune, notifier. Include log rotation and a bin/doctor.sh that checks paths/permissions.

⸻

P2 — Delta REST client + reports

Prompt P2.1 – Typed REST wrapper

Ship Phase 2 (Delta REST). Create exchange/delta_client.py with HMAC signing (method+timestamp+path+query+body → hex digest), retry/backoff, pagination helpers, and methods for positions, fills, wallet/balances, orders/history, and profile. Include minimal unit tests that stub responses.

Prompt P2.2 – Report pipeline refresh

Ship Phase 2 (Reports). Refactor the HTML/CSV/JSON report to use delta_client.py; normalize timestamp units; add “data snapshot” JSON alongside the HTML; keep the current CLI flags. Return code diffs and a test command that renders one hour to reports/ without emailing.

Prompt P2.3 – Mail sender harden

Ship Phase 2 (Email). Extract a reusable tools/send_report.py that supports Gmail (SSL 465 and STARTTLS 587), graceful errors, and a --dry-run. Patch the report script to call this helper. Provide a one-liner to test creds.

⸻

P3 — WebSocket notifier + Telegram

Prompt P3.1 – Realtime notifier

Ship Phase 3 (WebSocket). Build tools/delta_ws_notify.py that authenticates to Delta WS (/live), subscribes to orders, v2/user_trades, and positions, handles reconnects/heartbeats, dedupes by seq, and writes a rotating reports/notify.log.

Prompt P3.2 – Telegram connector

Ship Phase 3 (Telegram). Add tools/telepush.py and integrate it into the notifier so I get messages for: order placed/partially filled/filled/cancelled, and open-position size changes. Include env names to set (TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID) and a one-liner to auto-discover chat_id.

⸻

P4 — Risk & safety rails

Prompt P4.1 – Risk manager

Ship Phase 4 (Risk). Implement risk/limits.py + risk/config.yaml with: max daily loss, per-order notional cap, max concurrent orders, and per-symbol position cap. Add a kill_switch() that persists to state/kill.switch and is honored by order placement.

Prompt P4.2 – Order router guard

Ship Phase 4 (Router). Wrap all order placement in core/order_router.py that enforces risk rules and idempotency keys. Return code diffs plus a minimal CLI to “dry-run place” and see which rule blocks.

⸻

P5 — Observability

Prompt P5.1 – Structured logging

Ship Phase 5 (Logs). Add JSON logging via structlog with request IDs and order IDs, rotating files in logs/, and human-readable console output. Include a jq one-liner cheat-sheet to filter events.

Prompt P5.2 – Metrics surface

Ship Phase 5 (Metrics). Add a tiny /metrics HTTP server (Prometheus format) exposing heartbeat, last WS seq, queue depth, and PnL gauges. Provide docker-compose snippet to run Prometheus + Grafana locally.

⸻

P6 — Tests & sims

Prompt P6.1 – Unit/integration tests

Ship Phase 6 (Tests). Add pytest suite for: signer, pagination, time conversions, risk checks, and a mocked notifier flow. Give me make test that runs fast.

Prompt P6.2 – Offline simulator

Ship Phase 6 (Sim). Build a minimal “tape” simulator that replays historical fills from JSON and emits the same events the WS notifier would, so we can test alerts without live trading.

⸻

P7 — Packaging & deploy

Prompt P7.1 – Dockerize

Ship Phase 7 (Docker). Provide Dockerfile, docker-compose.yml, and .docker.env.example. Container should run bot, notifier, and cron tasks with bind-mounted reports/ and state/. Include upgrade steps.

Prompt P7.2 – Cloud checklist

Ship Phase 7 (Cloud). Give me a cloud deployment checklist (IP whitelist, secrets, systemd units, log shipping) for a single VM. Assume Ubuntu 22.04.

⸻

P8 — Strategy plugin system

Prompt P8.1 – Strategy interface

Ship Phase 8 (Plugins). Introduce strategies/base.py and refactor current grid into strategies/grid.py. Define a clean interface (init, on_tick, on_fill, on_timer). Add a sample “reverse” skeleton strategy file ready to implement.

⸻

P9 — Backtesting

Prompt P9.1 – Backtest harness

Ship Phase 9 (Backtest). Create a CLI backtester that feeds candles and synthetic fills into any strategy implementing the interface. Output equity curve CSV + simple matplotlib PNG.

⸻

P10 — CI, lint, docs

Prompt P10.1 – Pre-commit & CI

Ship Phase 10 (Quality). Add pre-commit with black/isort/flake8/mypy, and a GitHub Actions workflow running lint+tests. Include a CONTRIBUTING.md with commit style and branching.

Prompt P10.2 – Runbooks

Ship Phase 10 (Docs). Add RUNBOOK.md for: rotating keys, whitelisting IPs, recovering from kill-switch, and verifying end-to-end notifications.

⸻

“One-liner” umbrella prompts (when you want batches)
	•	Ship Phase 2 now
Ship all tasks from Phase 2 in ROADMAP.md. Give me code diffs, new files, and the exact commands to run locally to verify—no clarifying questions.
	•	Ship Phase 3 now
Ship Phase 3 (WS notifier + Telegram) end-to-end with a dry-run mode and reconnection logic. Include a single command I can run to see one test alert in Telegram.

⸻

When you paste a prompt…

If a task needs secrets, add at the top of your message:

Context for secrets: DELTA_API_KEY=⟨…⟩, DELTA_API_SECRET=⟨…⟩, TELEGRAM_BOT_TOKEN=⟨…⟩, TELEGRAM_CHAT_ID=⟨…⟩ are already in .env.reports.

That’s it—pick the next item you want, paste the corresponding prompt, and I’ll deliver the code + exact commands.