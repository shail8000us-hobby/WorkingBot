[pytest]
rootdir = .
# Prevent pytest from walking up to routes/__init__.py which triggers the Flask app
addopts = --import-mode=importlib
markers =
    sealed: contract tests for sealed functions — run before every deployment
