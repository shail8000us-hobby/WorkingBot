cd "$HOME/GridBot1.0.1"
python3 -m venv .venv
. .venv/bin/activate
python -m pip install -U pip setuptools wheel
pip install "ccxt>=4.5.5,<5" "requests==2.32.3" "python-dotenv==1.0.1" "websocket-client>=1.7.0,<2"

python - <<'PY'
import sys, ccxt, requests, dotenv, websocket
print("Python:", sys.version.split()[0])
print("ccxt:", ccxt.__version__)
print("requests:", requests.__version__)
print("dotenv OK:", hasattr(dotenv, '__all__'))
print("websocket-client:", websocket.__version__)
PY